# Dispatch Instructions

Read `SYSTEM.md` in this repository root. It is the canonical system prompt and project instruction source for Dispatch.

Always push completed branch work to `origin/main` and leave the local `main` branch behind; do not check out, merge into, or fast-forward local `main` just to publish work.
